From: Bernd Waibel <waebbl-gentoo@posteo.net>
Date: Sat, 28 Aug 2021 02:58:23 +0200
Subject: [PATCH] add cuda-11 compatibility

Signed-off-by: Bernd Waibel <waebbl-gentoo@posteo.net>
---
 CMakeLists.txt | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index b69912a..9392cba 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -575,8 +575,12 @@ if(CUDA_FOUND)
         if (NOT DEFINED OSD_CUDA_NVCC_FLAGS)
             if (CUDA_VERSION_MAJOR LESS 6)
                 set( OSD_CUDA_NVCC_FLAGS --gpu-architecture compute_11 )
-            else()
+            elseif (CUDA_VERSION_MAJOR LESS 9)
                 set( OSD_CUDA_NVCC_FLAGS --gpu-architecture compute_20 )
+            elseif (CUDA_VERSION_MAJOR LESS 11)
+                set( OSD_CUDA_NVCC_FLAGS --gpu-architecture compute_30 )
+            else()
+                set( OSD_CUDA_NVCC_FLAGS --gpu-architecture compute_35 )
             endif()
         endif()
     endif()
-- 
2.33.0

